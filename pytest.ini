[pytest]
testpaths = api_tests
norecursedirs = .git .pytest_cache __pycache__ node_modules archive api_tests/cross_platform
addopts = -ra
markers =
    cross_platform: 跨端验收场景，默认不纳入核心 API 烟测
    write: 写操作接口（执行后触发限频延时）
    rate_limited: 需要限频保护的接口用例
log_cli = true
log_cli_level = INFO
